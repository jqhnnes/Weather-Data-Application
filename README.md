# Weather-Data-Application
This project is a modular weather data application that fetches, stores, and periodically updates weather data for selected locations using a weather API. Users can query historical data via an API and visualize it in a simple UI, with optional features like user accounts, search, and map-based location selection.
